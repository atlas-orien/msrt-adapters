[package]
name = "msrt-std"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
readme = "README.md"
description = "std byte-stream adapters for MSRT."

[dependencies]
msrt = { workspace = true, features = ["std"] }

[lints]
workspace = true
