[package]
name = "msrt-adapter-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Shared adapter primitives for MSRT platform integrations."

[features]
default = ["std"]
std = ["msrt/std"]

[dependencies]
msrt = { workspace = true }
embedded-io-async = { workspace = true }

[lints]
workspace = true
